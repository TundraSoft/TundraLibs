{
  "compilerOptions": {
    "allowJs": false,
    "lib": [
      "deno.window"
    ],
    "strict": true,
    "experimentalDecorators": true
  },
  "unstable": ["fs", "net"],
  "lint": {
    "include": [
      "./"
    ],
    "exclude": [
      "*/tests/",
      "*/fixtures/"
    ],
    "rules": {
      "tags": [
        "recommended"
      ],
      "include": [
        "ban-untagged-todo"
      ],
      "exclude": []
    }
  },
  "fmt": {
    "include": ["./"],
    "exclude": ["*/tests/", "*/fixtures/", ".devcontainer", ".github"],
    "semiColons": true,
    "lineWidth": 80,
    "indentWidth": 2,
    "singleQuote": true,
    "proseWrap": "preserve"
  },
  "tasks": {
    "cache": "deno cache --reload ./dependencies.ts ./dev.dependencies.ts",
    "clean": "deno fmt && deno task cache",
    "bench": "deno bench -A --unstable-fs",
    "test": "deno test -A --parallel --unstable-fs --unstable-net",
    "check": "deno fmt --check && deno lint && deno check **/*.ts"
  },
  "test": {
    "include": [
      "./"
    ],
    "exclude": [
      "*/fixtures/"
    ]
  },
  "lock": false
}
