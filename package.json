{
  "compilerOptions": {
    "target": "ES2022",
    "experimentalDecorators": false
  },
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun run test:bun && bun run test:deno && bun run test:node",
    "test:bun": "bun test packages/",
    "test:deno": "deno test --allow-all --parallel",
    "test:node": "node --import tsx --test 'packages/**/*.test.ts'",
    "test:coverage:bun": "bun test --coverage packages/",
    "test:coverage:node": "node --import tsx --test --experimental-test-coverage 'packages/**/*.test.ts'",
    "fmt": "deno fmt",
    "lint": "deno lint"
  },
  "devDependencies": {
    "@std/asserts": "npm:@jsr/std__assert@^1.0.19",
    "@std/testing": "npm:@jsr/std__testing@^1.0.20",
    "tsx": "^4.21.0"
  },
  "license": "MIT"
}
