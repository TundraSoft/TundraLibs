name: rapid-blog-example
mode: DEVELOPMENT
server:
  port: 3100
  hostname: localhost
  paging:
    defaultSize: 10
    maxSize: 50
  query:
    maxFilters: 10
    maxSorts: 3
  versioning:
    header: x-api-version
    default: v1 # a plain request (no header) resolves here — find() also has a v2
