/**
 * @fileoverview `negotiate` — HTTP content negotiation over an `Accept`
 * header. The implementation was PROMOTED to `@tundralibs/compat/http`
 * (REVIEW-3); this re-export keeps the package's barrel path stable.
 *
 * @module
 */

export { negotiate } from '@tundralibs/compat/http';
