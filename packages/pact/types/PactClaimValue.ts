/**
 * @fileoverview Sanitized claim value for `@tundralibs/pact` — what a
 * declared OAuth claim casts to.
 *
 * @module
 */

/** A sanitized claim value (see `PactClaimSpec` for the cast rules). */
export type PactClaimValue = string | number | boolean | Date;
