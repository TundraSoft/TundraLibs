/**
 * Events emitted by a Pact instance. Placeholder set while the package is
 * rebuilt — the real event surface lands with the auth flows.
 */
export type PactEvents = {
  test: (message: string) => void;
};
